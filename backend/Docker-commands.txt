# solarboat2.0
Website of the Solar Boat Team


```sh
docker build -t solarboat .
```
> Build project locally

```sh
docker save -o solarboat_image solarboat
```
> Save locally built image

```sh
docker load -i solarboat_image
```
> Load saved image

```sh
docker run -d --rm --name sb_container -p 81:8080 -v sql:/var/lib/mysql solarboat  
docker run -d --rm --name sb_container -p 81:8080 -v sql:/var/lib/mysql -v app:/var/www/html/assets solarboat
docker run -d --rm --name sb_container -p 81:8080 -v sql:/var/lib/mysql -v /usr/src/app:/var/www/html/assets solarboat 
```
> Run image



docker image/container prune



1. Docker buildes parancs
2. docker save images parqncs.
-> megjelenik a mappaban az image
3.atkopizod az imaget a serverre a winscpvel. A root mappaba kell tenni, ha latsz sok masik imaget akkor vagy jo helyen😃
4. Puttyban: docker load images parancs
5. Leallitod a most futó containert: "docker stop container"
6. Elinditasz egy kontenert a mostani imaggel: puttyban docker run valami nagyon hosszu parancs es a legvegere beleirod a betoltott image nevet

docker image ls
docker rmi *imageneve*